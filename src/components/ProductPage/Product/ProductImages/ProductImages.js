import React from 'react';
import classes from './ProductImages.scss';

const ProductImages = () => (
    <div className={classes.ImagesContainer}>
        <div className={classes.ImageHolder} />
    </div>
);

export { ProductImages };
